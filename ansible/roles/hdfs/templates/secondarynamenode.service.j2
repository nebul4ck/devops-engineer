[Unit]
Description=HDFS - SecondaryNamenode Service
Documentation=https://https://hadoop.apache.org
After=network.target

[Service]
Type={{ hdfs_systemd_type|default('forking') }}

User={{ hdfs_user|default('hdfs') }}
Group={{ hdfs_group|default('hdfs') }}

ExecStart={{ hdfs_base_dir }}/sbin/hadoop-daemon.sh start {{ secondary_namenode_service|default('secondarynamenode') }}
ExecStop={{ hdfs_base_dir }}/sbin/hadoop-daemon.sh stop {{ secondary_namenode_service|default('secondarynamenode') }}

[Install]
WantedBy=multi-user.target