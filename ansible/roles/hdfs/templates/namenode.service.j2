[Unit]
Description=HDFS - NameNode Service
Documentation=https://https://hadoop.apache.org
After=network.target

[Service]
Type={{ hdfs_systemd_type|default('forking') }}

User={{ hdfs_user|default('hdfs') }}
Group={{ hdfs_group|default('hdfs') }}

ExecStart={{ hdfs_base_dir }}/sbin/hadoop-daemon.sh start {{ namenode_service|default('namenode') }}
ExecStop={{ hdfs_base_dir }}/sbin/hadoop-daemon.sh stop {{ namenode_service|default('namenode') }}

[Install]
WantedBy=multi-user.target